from dotenv import load_dotenv
import os
from pathlib import Path

# Загружаем файл .env из текущей папки
load_dotenv(dotenv_path=Path('.') / '.env')

# Проверяем переменную
vasya = os.getenv("VASYA")
print("VASYA =", vasya)

